{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C++",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",                // отладочная информация
                "main.cpp",         // твой исходный файл
                "-o",
                "main.exe"          // итоговый exe (будет в той же папке)
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Компиляция C++ через MinGW-w64"
        }
    ]
}
